[tool.poetry]
name = "snaptastic"
version = "2.0"
description = "Photo booth"
authors = ["Ray Luna <ray@hello-upswell.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
fastapi = "^0.115.5"
uvicorn = "^0.32.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
